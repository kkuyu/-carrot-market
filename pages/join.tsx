import { NextPage } from "next";

import Layout from "@components/layout";

const Join: NextPage = () => {
  return (
    <Layout hasHeadBar={false}>
      <div>Join</div>
    </Layout>
  );
};

export default Join;
